class_name WorldMemory
extends RefCounted

var world_seed:int=0

## уже созданные декачанки

var generated_decachunks={}

## измененные

var changed_decachunks={}

## созданные чанки

var generated_chunks={}

## измененные чанки

var changed_chunks={}

static func key(v:Vector2i)->String:

	return str(v.x)+","+str(v.y)

static func parse(key:String)->Vector2i:

	var p:=key.split(",")

	return Vector2i(
		int(p[0]),
		int(p[1])
	)